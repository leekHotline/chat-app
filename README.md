# chat-app
Build a modern AI chat application that supports multiple models, MCP services, streaming responses, and exquisite animations.构建一个现代化的 AI 聊天应用，支持多模型、MCP 服务、流式响应和精美动画。

for an example

uv sync
uv add package
uv run xxx.py
curl -s https://jsonplaceholder.typicode.com/users | jq -r '.[].name'

# 安装pnpm包管理器 创建nextjs项目
pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"